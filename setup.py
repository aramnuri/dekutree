
from setuptools import setup, find_packages

setup(name='dekutree',
      version='0.1.0',
      url='https://github.com/aramnuri/dekutree',
      packages=find_packages(),
      install_requires=['pandas'])
